enable-pre-post-scripts=true
# Required for `pnpm deploy` (used in apps/api and apps/web Dockerfiles) to
# bundle workspace:* deps (e.g. @locus/shared) as real files instead of
# symlinks, so the deployed dir is self-contained in the final Docker stage.
inject-workspace-packages=true
