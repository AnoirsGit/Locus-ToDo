# Branch: front-initial — DONE

All tasks complete. 0 svelte-check errors.

## Lessons

- `$bindable()` in Svelte 5: keep state as `string` in form, convert to typed values at submit boundary
- Feature UI must NOT import from its own slice's `index.ts` (circular dep)
- Entity constants (`MONTH_NAMES_*`, `DAY_NAMES_SHORT`) live in `entities/task/model/task.constants.ts`
- `$derived.by(() => { ... })` for multi-line derivations in Svelte 5
- SvelteKit routes dir is configurable: `kit.files.routes: 'src/pages'` in `svelte.config.js`
